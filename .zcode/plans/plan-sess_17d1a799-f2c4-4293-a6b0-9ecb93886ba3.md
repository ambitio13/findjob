# 油猴插件 Bridge 架构 — Trellis 同步 + 规约更新 + 实现

## 第一步：Trellis 任务同步

当前会话指针指向旧任务 `08-01-post-mvp-application-readiness-loop`。需要：

1. **创建新任务** `08-02-userscript-bridge-adapter`：
   ```bash
   python3 ./.trellis/scripts/task.py create "油猴脚本桥接适配器替换 CDP/Playwright" --slug userscript-bridge-adapter
   ```

2. **写规划文档**到 `.trellis/tasks/08-02-userscript-bridge-adapter/`：
   - `prd.md` — 需求：替换 CDP 方式为油猴桥接，后端发指令油猴执行，绕过 BOSS CDP 检测；所有安全不变量保留
   - `design.md` — 技术设计：内存指令队列、UserscriptBossAdapter、HTTP 桥接端点、油猴脚本、前端状态指示器
   - `implement.md` — 执行清单：9 个步骤（channel → adapter → API → config/registry → userscript → frontend → docs → tests）

3. **配置 context jsonl**（`implement.jsonl` + `check.jsonl`）— 引入相关 spec 文件

4. **激活任务**（`task.py start`）

## 第二步：规约同步

更新 `.trellis/spec/` 中的以下文件：

### `big-question/platform-integration-strategy.md`
- 新增"BOSS 直聘反自动化检测发现"小节：记录 CDP 协议层检测（Page.navigate / Runtime.evaluate 被检测），累积式标记
- 新增"油猴脚本桥接方案"小节：页面内执行无 CDP 签名，后端发指令油猴执行，内存指令队列不持久化
- 更新 Preferred Order：在 "Browser automation behind platform-specific adapters" 下补充"优先使用浏览器扩展/油猴脚本而非 CDP"

### `backend/api-contracts.md`
- 新增"桥接端点模式"：无认证端点（油猴脚本不带 X-User-Id）、long-poll 取指令、POST 回传结果、心跳维持连接

### `backend/authentication.md`
- 新增"桥接通道安全"：指令队列纯内存不持久化、回传结果只含脱敏值、navigate 指令限制为只读校验

## 第三步：代码实现

按 `implement.md` 中的 9 步执行：

| # | 文件 | 改动 |
|---|------|------|
| 1 | `backend/app/platforms/boss/userscript_channel.py` | 新建：内存指令队列（Instruction/Result/Channel） |
| 2 | `backend/app/platforms/boss/userscript_adapter.py` | 新建：UserscriptBossAdapter + UserscriptBossPage |
| 3 | `backend/app/schemas/userscript_bridge.py` | 新建：Pydantic 模型 |
| 4 | `backend/app/api/v1/userscript_bridge.py` | 新建：4 个端点（status/next-instruction/result/heartbeat） |
| 5 | `backend/app/api/v1/router.py` | 注册新 router |
| 6 | `backend/app/core/config.py` | 新增 `boss_userscript_bridge_enabled` |
| 7 | `backend/app/platforms/boss/registry.py` | 三路选择：userscript > real > fake |
| 8 | `docs/boss-userscript.user.js` | 新建：Tampermonkey 用户脚本 |
| 9 | 前端 | `useBridgeStatus.ts` + GuidedSubmitPanel 状态指示 |
| 10 | `docs/manual-boss-pilot.md` | 更新 runbook |

## 第四步：测试

- 新建 `test_userscript_boss_adapter.py`（注入 FakeChannel，测试 prepare/submit 全路径）
- 新建 `test_userscript_bridge_api.py`（端点测试）
- 修改 `test_platform_boss_adapter.py`（registry 三路选择）
- 现有 522 测试保持不变

## 安全不变量（全部保留）

- 一次调用只处理一个 application_id
- prepare 永不点击最终提交（UserscriptBossPage.click 在 prepare 阶段 raise）
- submit 恰好点击一次
- 指令队列纯内存，不持久化
- 回传结果只含脱敏值（visible/count/title/url_hash/error）
- navigate 指令限制为只读校验（不自动跳转）
- 选择器从后端下发，油猴脚本不硬编码
- 审批边界、幂等键、跨用户 404 不变（service 层不变）
- userscript 模式不加载 Playwright